---
title: About Me
---

import { Image, Timeline } from "@chakra-ui/react";
import { makeMilestoneCard } from "@/app/ui/milestone-card-server";
import MilestoneCard from "@/app/ui/milestone-card";
import { getMilestonesPath, getRawMilestoneMetadata } from "@/app/(pages)/_content/milestones";
import HelloName from "@/app/ui/hello-name";
import PinCategory from "@/app/ui/pin-category";
import PinLabel from "@/app/ui/pin-label";
import PinList from "@/app/ui/pin-list";
import {
  PythonPin,
  JsTsPin,
  RustPin,
  CppPin,
  TensorflowPin,
  ScikitPin,
  FlaskPin,
  DjangoPin,
  NextJsPin,
  NestJsPin,
  DockerPin,
  CoolifyPin,
  LinuxPin,
  OpencodePin,
} from "@/app/ui/pin";

# About Me

Hello, my name is <HelloName />.

I'm a programmer who has had at least 6 years of programming by this point. My main languages are Python, Rust, C and recently JavaScript and TypeScript. I have also dabbled in competitions in fields like Cybersecurity and AI, as well as contributed to a few coding events in Singapore.

## Skills

<PinCategory label={<PinLabel>Languages</PinLabel>}>
  <PinList>
    <PythonPin />
    <JsTsPin />
    <RustPin />
    <CppPin />
  </PinList>
</PinCategory>

<PinCategory label={<PinLabel>AI</PinLabel>}>
  <PinList>
    <TensorflowPin />
    <ScikitPin />
  </PinList>
</PinCategory>

<PinCategory label={<PinLabel>Frameworks</PinLabel>}>
  <PinList>
    <FlaskPin />
    <DjangoPin />
    <NextJsPin />
    <NestJsPin />
  </PinList>
</PinCategory>

<PinCategory label={<PinLabel>Systems</PinLabel>}>
  <PinList>
    <DockerPin />
    <CoolifyPin />
    <LinuxPin />
  </PinList>
</PinCategory>

<PinCategory label={<PinLabel>Agents</PinLabel>}>
  <PinList>
    <OpencodePin />
  </PinList>
</PinCategory>

## Milestones

<Timeline.Root variant="solid">
  {[
    "events/cssxcdac",
    "events/buildingblocs2023",
    "competitions/cyberthon2023",
    "competitions/naisc2024"
  ]
    .map(async (p, i) => {
      const rawMetadata = await getRawMilestoneMetadata(`./${p}/page.mdx`);

      return <Timeline.Item key={p}>
        <Timeline.Connector>
          <Timeline.Separator />
          <Timeline.Indicator>{i + 1}</Timeline.Indicator>
        </Timeline.Connector>

        <Timeline.Content>
          <Timeline.Title>
            {rawMetadata.title}
          </Timeline.Title>
          {await makeMilestoneCard(p, { className: "w-full" })}
        </Timeline.Content>
      </Timeline.Item>;
  })}
</Timeline.Root>

## Education

<Timeline.Root variant="solid">
  {[
    "education/ri",
    "education/nus",
  ]
    .map(async (p, i) => {
      const rawMetadata = await getRawMilestoneMetadata(`./${p}/page.mdx`);

      return <Timeline.Item key={p}>
        <Timeline.Connector>
          <Timeline.Separator />
          <Timeline.Indicator>{i + 1}</Timeline.Indicator>
        </Timeline.Connector>

        <Timeline.Content>
          <Timeline.Title>
            {rawMetadata.title}
          </Timeline.Title>
          {await makeMilestoneCard(p, { className: "w-full" })}
        </Timeline.Content>
      </Timeline.Item>;
  })}
</Timeline.Root>
